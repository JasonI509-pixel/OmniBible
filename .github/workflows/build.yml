name: Build Android App Bundle

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install pnpm
      uses: pnpm/action-setup@v3
      with:
        version: 8

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    - name: Install Dependencies
      run: pnpm install

    - name: Build Web Application
      run: pnpm --filter client build

    - name: Initialize Mobile Wrapper
      run: |
        cd client
        pnpm add @capacitor/core @capacitor/cli @capacitor/android
        pnpx cap init "Manus Web App" "com.manus.app" --web-dir="dist"
        pnpx cap add android

    - name: Sync Production Build
      run: |
        cd client
        pnpx cap sync

    - name: Build Android App Bundle (.aab)
      run: |
        cd client/android
        ./gradlew bundleRelease
        
    - name: Upload .aab Artifact
      uses: actions/upload-artifact@v4
      with:
        name: android-app-bundle
        path: client/android/app/build/outputs/bundle/release/app-release-unsigned.aab
